name: "[API] lint, compile TS"

on:
  push:
    paths:
      - "api/**"
      - ".github/workflows/api.yml"
  pull_request:
    types: ["opened", "edited", "synchronize"]
    paths:
      - "api/**"
      - ".github/workflows/api.yml"

jobs:
  lint-build:
    name: lint and compile TS
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn lint
      - run: yarn ts
